There is dataset in four text types: interview, biographics, wiki-how and mixture.
The first three text types includes several documents seperately.
Documents in each text type should be combined firstly and then separate into training, validation and test set (not presented in code).
The mixture dataset is called eng.rst.gum. It has been separated thoroughly.
